# Copy this file to .env and fill in your values.
# SUPABASE_URL and SUPABASE_ANON_KEY are public by design (protected by RLS).
# DeepL and Gemini keys are stored as Supabase secrets — never in the client.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
